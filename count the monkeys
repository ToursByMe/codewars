function monkeyCount(n) {
// array's declaration
let monky = [];
//keep it simple stupid!!!
 for( let i = 1; i <= n; ++i){
  console.log( monky.push(i));
  //start stop using console.log!!!
  
}// Why I always forget the return?!! Half morning wasted you idiotic geek!
return monky;
};

monkeyCount(10);

/******** refactor power ********/

const monkeyCount = (n) => [...Array(n).keys(n)].map((data) => data + 1);
